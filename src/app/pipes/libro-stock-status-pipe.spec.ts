import { LibroStockStatusPipe } from './libro-stock-status-pipe';

describe('LibroStockStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new LibroStockStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
