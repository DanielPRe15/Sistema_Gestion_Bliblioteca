import { DiasRestantesPipe } from './dias-restantes-pipe';

describe('DiasRestantesPipe', () => {
  it('create an instance', () => {
    const pipe = new DiasRestantesPipe();
    expect(pipe).toBeTruthy();
  });
});
